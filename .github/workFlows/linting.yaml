name: linting

on: pull_request

jobs:
  prettier:
    name: prettier
  runs-on: ubuntu-latest
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: "lts/hydrogen"
    - run: |
        npm ci
        npm run
