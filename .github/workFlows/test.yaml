name: Automated Tests

on: pull_request

jobs:
  name: Jest Ubuntu
  runs-on: ubuntu-latest
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: "lts/hydrogen"
    - run: |
        npm ci
        npm test
